/**
 * UI configuration.
 *
 * Edit these values to change UI timing and display behaviour
 * without touching any logic files.
 */

/** How long (ms) a quest/delivery toast stays visible before auto-dismissing. */
export const TOAST_DURATION_MS = 3200
